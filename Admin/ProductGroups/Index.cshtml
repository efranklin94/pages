@page "/Admin/ProductGroups"
@model kamjaService.Pages.Admin.ProductGroups.IndexModel

@{
    ViewData["Title"] = "Index";
}
@{
    Layout = "/Pages/Shared/_Layout.cshtml";
}
<h3>سرویس ها</h3>

<table class="table table-hover table-striped  ">
    <thead>
        <tr>
            <th>

            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProductGroup)
        {
            <tr onclick="location.href = '@(Url.Page("/Admin/ProductGroupings/Editable", new { id = @item.ProductGroupId }))'">
                <td>

                    @Html.DisplayFor(modelItem => item.Expr1)
                </td>


            </tr>
        }
    </tbody>
</table>
