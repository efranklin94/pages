@page
@model kamjaService.Pages.Admin.SolidImages.Step1Model

@{
    Layout = "~/Pages/Shared/_Layout.cshtml";
}
<table class="table table-hover table-striped  ">
    <thead>
        <tr>
            <th>

            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Parent)
        {
            <tr onclick="location.href = '@(Url.Page("/Admin/SolidImages/Step2/Index", new { id = @item.ParentId }))'">
                <td>

                    @Html.DisplayFor(modelItem => item.Name)
                </td>


            </tr>
        }
    </tbody>
</table>