@page
@model kamjaService.Pages.Admin.ParentGroup.IndexModel
@{
}
<table class="table table-hover table-striped  ">
    <thead>
        <tr>
            <th>

            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Parent)
        {
            <tr onclick="location.href = '@(Url.Page("/Admin/ProductGroups/Index", new { id = @item.ParentId }))'">
                <td>

                    @Html.DisplayFor(modelItem => item.Name)
                </td>


            </tr>
        }
    </tbody>
</table>