@page "/Salesinfo/ProductGroups"
@model kamjaService.Pages.ProductGroups.IndexModel

@{
    ViewData["Title"] = "Index";
}
@{
    Layout = "/Pages/Shared/_Layout.cshtml";
}
<h3>سرویس ها</h3>

<table class="table table-hover table-striped  ">
    <thead>
        <tr>
            <th>

            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProductGroup)
        {
            <tr onclick="location.href = '@(Url.Page("/Salesinfo/ProductGroupings/Index", new { id = @item.ProductGroupId }))'">
                <td>

                    @Html.DisplayFor(modelItem => item.Expr1)
                </td>


            </tr>
        }
    </tbody>
</table>

@*<div class="row">

    @foreach (var item in Model.ProductGroup)
    {
        <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12 pull-right">
            <div class="figure-grid">
                <div class="image" style="overflow:hidden;height:auto">
                    <a href="/Salesinfo/ProductGroupings?id=@item.ProductGroupId ">
                        <img src="~/image/Product/12071453325241.jpg" />
                    </a>
                </div>
                <div class="text">
                    <a href="/Salesinfo/ProductGroupings?id=@item.ProductGroupId "> @Html.DisplayFor(modelItem => item.Expr1)</a>
                </div>
            </div>
        </div>

    }

</div>*@

@*<table class="table table-hover table-striped ">
    <thead>
        <tr>
            <th>

            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProductGroup)
        {
            <tr onclick="location.href = '@(Url.Page("/ProductGroupings/index", new { id = @item.ProductGroupId }))'">
                <td>

                    @Html.DisplayFor(modelItem => item.Expr1)
                </td>


            </tr>
        }
    </tbody>
</table>*@
